# ============================================
# Aurora Social Server - Environment Variables
# Copy this file to .env and update the values
# ============================================

# ---- Database ----
# Local development:
DATABASE_URL="postgresql://postgres:yourpassword@127.0.0.1:5433/aurora_social?schema=public"
# AWS RDS example:
# DATABASE_URL="postgresql://postgres:yourpassword@your-rds-endpoint.amazonaws.com:5432/aurora_social?schema=public"

# ---- Server ----
PORT=5000
NODE_ENV=development

# ---- CORS ----
# Comma-separated list of allowed frontend origins
# Leave empty to default to localhost:5173,5174
# ALLOWED_ORIGINS=https://your-frontend.com,https://www.your-frontend.com

# ---- JWT ----
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_generate_a_secure_secret
JWT_EXPIRES_IN=7d

# ---- AWS S3 (Production file uploads) ----
# Leave empty to use local disk storage in development
# AWS_S3_BUCKET=aurora-social-uploads
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key (optional if using IAM roles on EC2/ECS)
# AWS_SECRET_ACCESS_KEY=your_secret_key (optional if using IAM roles on EC2/ECS)

# ---- News API ----
# Get a free key from https://newsapi.org/account
NEWS_API_KEY=your_news_api_key_here
